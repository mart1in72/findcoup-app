name: Deploy Flet App to Pages

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5 # Обновлено до v5
        with:
          python-version: '3.10'
          
      - name: Install Dependencies
        run: |
          pip install flet
          pip install -r requirements.txt
          
      - name: Build Web App
        run: |
          # Собираем проект в папку dist
          flet publish main.py --base-url /${{ github.event.repository.name }}/
          
      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3 # Исправлено на v3 (для Pages это актуальная версия)
        with:
          path: dist/

  deploy:
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4 # Исправлено на v4
